// Buffer Line
import { StyleSheet } from "react-native";
import { flex, full, row } from "./StyleSheetHelper";

const LoginPageStyleSheet = StyleSheet.create({
  input: {
    margin: 12,
    borderBottomWidth: 1,
    borderRadius: 10,
    padding: 10,
  },
});

export default LoginPageStyleSheet;
