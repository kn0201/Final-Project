import { StyleSheet } from "react-native";
import { center, flex } from "./StyleSheetHelper";

const MemoryScreenStyleSheet = StyleSheet.create({
  center: {
    display: flex,
    justifyContent: center,
    alignItems: center,
  },
});

export default MemoryScreenStyleSheet;
