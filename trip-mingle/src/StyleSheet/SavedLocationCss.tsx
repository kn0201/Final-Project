//Buffer Line
import { StyleSheet } from "react-native";
import { center, flex } from "./StyleSheetHelper";

const SavedLocationStyleSheet = StyleSheet.create({
  center: {
    display: flex,
    justifyContent: center,
    alignItems: center,
  },
});

export default SavedLocationStyleSheet;
